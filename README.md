# Neural-Network-1.0
Its a basic neural network built from scratch in python. It user RELU and softmax as activation functions and calculates loss using Categorical Cross entropy function. Currently I have trained it for the Mnist and the fashion MNist dataset.
